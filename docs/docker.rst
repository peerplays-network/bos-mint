## Docker

### Build and Run Docker Container

.. code-block::

    $ cd bookied-ui
    $ docker build -t boss:latest .
    $ docker-compose up

then open browser with URL:

    http://localhost:8000
